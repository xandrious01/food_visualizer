import React, { createContext } from "react";

export const CompareFoodsContext = React.createContext();

export const TriggerReloadContext = React.createContext();

export const NetworkErrorContext = React.createContext();