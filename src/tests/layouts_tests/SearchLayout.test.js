import React from 'react';
import { render, screen } from '@testing-library/react';
import SearchLayout from '../../layouts/SearchLayout';

test('renders SearchLayout correctly', () => {
  render(<SearchLayout />);
}); 